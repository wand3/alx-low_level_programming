#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * @n: the number of which the absolute value will be printed
 * Return: n
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (n * -1);
}
